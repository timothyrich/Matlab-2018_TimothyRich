function v1 = EXBalance1(n,n1);	% EXBalance1 fills and randomized order of one vector% vectors have length n, first has values 1..n1, second has values 1..n2 % pass in:  vector length, n1% returns:  	the two vectors% 5/30/02		EXBalance2 pulled out of old program% 7/09/02		replaced EXPermute with Shuffle% 2/27/03		created EXBalance1npc = round(n/n1);			if npc*n1 ~= n						% check in number of trials OK?	error('EXBalance1 ERROR:  bad number of conditions'); end;% fill in first vectornpc = round(n/n1);	v1 = [];for i=1:n1	v1 = [v1,repmat(i,1,npc)];		end;v1 = Shuffle(v1);					% permute elements of vector